{
  "mcpServers": {
    "nexus-crm": {
      "type": "http",
      "url": "http://localhost:3000/api/mcp",
      "headers": {
        "Authorization": "Bearer ${NEXUS_MCP_API_KEY}"
      }
    }
  }
}
